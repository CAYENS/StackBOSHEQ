export default function Loading() {
  return (
    <div className="site-offset flex min-h-[60vh] items-center justify-center bg-blackish">
      <div className="text-center">
        <p className="font-heading text-[50px] uppercase tracking-[0.05em]">BOSHEQ</p>
        <p className="mt-2 text-sm uppercase tracking-[0.2em] text-milk/70 animate-pulse">Loading atmosphere…</p>
      </div>
    </div>
  );
}
